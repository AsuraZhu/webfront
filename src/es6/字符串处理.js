// js常用的一些字符串处理
//去空格
"   abc  ".replace(/(^\s*)|(\s*$)/g,'');
"   abc   ".trim();

[].concat();
[].join();
[].splice();
[].slice();

/**
 * join [123].join() //1,2,3 默认   [1,2,3].join()   // 1-2-3
 * push pop  
 * shift unshift  //在首部添加 和删除  和push pop相反
 * sort 排序
 * reverse
 * concat [1,3,5,7].concat(9) 原数组不变    返回新数组 [1,3,5,7,9]
 * slice 
 * splice
 * indexof
 * forEach
 * map
 * filter
 * every
 * some
 * reduce
 * reduceRight
 * 
 */

 /**
  * 字符串
    indexOf 索引
    chartAt
    match  //匹配
    substr //截取字符串
    replace // 替换查找
    serach  //查找
    slice  //
    split //划分字符串
    toLowerCase 
    toUpperCase


  */